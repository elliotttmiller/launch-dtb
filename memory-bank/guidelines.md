# Drywall Toolbox — Guidelines Compatibility Note

This file is retained only for compatibility with older sessions/bookmarks. It is **not** a normative engineering source.

Repository-wide rules live in `AGENTS.md`. Model-neutral reusable AI roles, skills and workflows live under `.agents/`. Domain-specific implementation contracts belong in active source and current owning documentation under `docs/`.

Do not add new programming, security, checkout, deployment or assistant instructions here. If a durable rule changes, update its canonical owner instead.
