{
  "name": "drywall-toolbox-email-previewer",
  "version": "2.0.0",
  "private": true,
  "description": "Standalone BrowserSync workspace for rapid DTB transactional email design previews.",
  "scripts": {
    "dev": "browser-sync start --config browser-sync.config.cjs"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  },
  "engines": {
    "node": ">=20"
  }
}
