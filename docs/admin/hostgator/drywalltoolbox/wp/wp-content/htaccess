# wp-content should never run permalink rewrites.
# Permalink routing belongs in /wp/.htaccess.

Options -Indexes

<Files ".ht*">
<IfModule mod_authz_core.c>
Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
Deny from all
</IfModule>
</Files>
